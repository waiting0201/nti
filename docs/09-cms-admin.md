# 09 · 後台 CMS — 功能規格書

| 欄位 | 內容 |
|------|------|
| **主責 Agent** | `backend-engineer`（API + 後台 SPA）／`system-analyst`（權限與欄位定義） |
| **對應階段** | P4（CMS）／P6（會員・表單） |
| **技術基線** | 後台為 **純 SPA（靜態、noindex）**，資料一律走 `/api/v1/admin/*`（Azure Functions .NET 10 + Dapper） |
| **配套文件** | [08-database.md](08-database.md)（資料表）、[04-api.md](04-api.md)（契約）、[05-seo.md](05-seo.md)（SEO 欄位） |
| **資料來源** | `mockup/` 44 頁實際結構（2026-09-01 定案版）、[官網資訊架構 IA](../reference/官網資訊架構_IA.md) |

---

## 1. 三條專案決議（本文件的裁切依據）

1. **後台以單元功能為主** — 一個前台內容區塊 = 一個後台單元，各自獨立的清單／編輯畫面。不做通用 page-builder、不做區塊拖拉組版。
2. **不做 Media Library** — 沒有「媒體管理」選單。圖片／檔案只能從**所屬欄位**上傳，隨該筆資料建立與刪除；**每個上傳欄位旁必須顯示建議尺寸提示**（§3 為權威文字）。
3. **固定文字區不做後台** — 品牌敘述型的長篇文案寫死在前端（清單見 §7），後台只保留這些頁的 SEO 欄位。

---

## 2. 後台選單與單元總表

| 群組 | # | 單元 | 代號 | 主要資料表 | 階段 |
|------|---|------|------|-----------|------|
| 儀表板 | 00 | 待辦總覽 | `dashboard` | — | P4 |
| 首頁 | 01 | 首頁 Banner | `home-banner` | `HomeBanner` | P4 |
| 內容 | 02 | 解決方案 Solutions | `solution` | `Solution`／`SolutionItem` | P4 |
| 內容 | 03 | 案例實績 Projects | `project` | `Project` | P4 |
| 內容 | 04 | 最新消息 News | `news` | `News` | P4 |
| 內容 | 05 | Green Vlog | `vlog` | `Vlog` | P4 |
| 內容 | 06 | FAQ | `faq` | `Faq` | P4 |
| 內容 | 07 | 產業趨勢 Industry Trends | `trend` | `IndustryTrend` | P4 |
| 內容 | 08 | 認證・夥伴・獎項 | `certification` | `Certification` | P4 |
| 內容 | 09 | 客戶 Logo | `client` | `ClientLogo` | P4 |
| 內容 | 10 | 設備與廠房 | `facility` | `FacilityItem` | P4 |
| 內容 | 11 | 職缺 Careers | `job` | `JobPosting` | P4 |
| 供應商 | 12 | 供應商公告 | `supplier-notice` | `SupplierNotice` | P4 |
| 供應商 | 13 | 規格與要求 | `supplier-spec` | `SupplierSpec` | P4 |
| 供應商 | 14 | 下載專區 | `supplier-download` | `SupplierDownload` | P4 |
| 頁面／SEO | 15 | 頁面設定與 SEO | `page` | `Page` | P4 |
| 頁面／SEO | 16 | 301 轉址 | `redirect` | `Redirect` | P8 |
| 表單 | 17 | 報價需求 | `quote` | `QuoteRequest` | P6 |
| 表單 | 18 | 聯絡訊息 | `contact` | `ContactMessage` | P6 |
| 會員 | 19 | 會員管理 | `member` | `Member` | P6 |
| 會員 | 20 | 訂單與生產進度 | `order` | `Orders`／`OrderProgress` | P6 |
| 系統 | 21 | 網站設定 | `setting` | `SiteSetting` | P4 |
| 系統 | 22 | 分類管理 | `category` | `Category` | P4 |
| 系統 | 23 | 管理員與角色 | `admin` | `AdminUser`／`Role` | P4 |
| 系統 | 24 | 操作紀錄 | `audit` | `AuditLog`／`EmailLog` | P4 |


### 2.1 與規劃書 §3-1 功能清單的差異

規劃書（2026-06）的 CMS 功能清單有 22 項，與本文件 24 個單元逐項比對後，**只有下列不是一對一對應**，其餘 13 項照收：

| 規劃書項目 | 現況 | 原因 |
|---|---|---|
| 首頁 COURAGE 區塊維護 | 移除 | 2026-09-01 首頁改版後 COURAGE 併入 Banner 標語，屬固定文案（§7） |
| 首頁 Project 精選維護 | 移除 | Featured Projects 整組已被 Proof 認證牆取代（[IA §6](../reference/官網資訊架構_IA.md)）→ 改由單元 08 維護 |
| NTI Difference 維護 | 拆分 | 敘述文字固定（§7）；認證／夥伴／獎項 → 單元 08 |
| Advantages 綠色優勢維護（圖文數據） | 改為固定 | 四個 Sustainability 內頁為品牌長文，含水處理數值等（§7） |
| 首頁 Banner 支援**影片**上傳 | 僅圖片 | mockup hero 為圖片輪播；要影片需追加欄位與播放器 |
| Green Vlog／**電子報** | 僅 Vlog | 電子報未納入，見下方缺口 |
| **電子報訂閱名單管理** | 未納入 | 見下方缺口 |
| **CSR 社會責任維護** | 未納入 | mockup 44 頁中無 CSR 頁 |
| 語系管理（中英對照） | 不設獨立單元 | 改為每個單元的中／英分頁 + 清單完成度 badge（§5.3） |

規劃書未列、本文件新增的單元，來自 2026-08-31 IA 改版與 SEO 需求：
`faq`、`trend`、`job`、`page`、`redirect`、`category`、`audit`、`order`、`dashboard`。

> ⚠️ **三個待客戶確認的缺口**（在規劃書內、mockup 外）：**電子報**（含訂閱名單管理與舊站名單遷移）、**CSR 頁**、**首頁 Banner 影片**。
> 三者屬範圍變更，**未確認前不納入本期估算**（後台單元、前端與工時皆不含）。
>
> **schema 已先預留**（2026-09-02），客戶點頭後不必再動資料結構，只需補後台單元與權限碼：
>
> | 缺口 | 已預留 | 位置 |
> |---|---|---|
> | 電子報 | `NewsletterSubscriber` 表（double opt-in、`Source='Import'` 支援舊站名單匯入） | [08-database.md §4.15](08-database.md) |
> | 首頁 Banner 影片 | `HomeBanner.MediaType` / `VideoPath` + 兩條 CHECK | [08-database.md §4.2](08-database.md) |
> | CSR 頁 | `Page` 第 29 筆 `green-csr`（`HasRichBody = 1`、`IsIndexable = 0`） | [08-database.md §6.4](08-database.md) |

---

## 3. 上傳欄位與建議尺寸（**權威文字，逐字顯示在欄位旁**）

| 單元 | 欄位 | 欄位旁提示文字 |
|------|------|---------------|
| `home-banner` | 桌機圖 | 建議 **2400×900px**（16:6）｜JPG／WebP｜≤500KB |
| `home-banner` | 手機圖（選填） | 建議 **1080×1350px**（4:5）｜JPG／WebP｜≤300KB｜未上傳則沿用桌機圖 |
| `setting` | 首頁形象圖帶 | 建議 **2400×1000px**（12:5）｜JPG／WebP｜≤500KB |
| `solution` | 方案封面 | 建議 **1160×940px**（約 5:4）｜JPG／WebP｜≤300KB｜同時用於首頁 Printing Solutions 卡片 |
| `solution` | 品項圖 | 建議 **1280×960px**（4:3）｜JPG／WebP｜≤300KB |
| `project` | 案例圖 | 建議 **1160×940px**（約 5:4）｜JPG／WebP｜≤300KB |
| `news` | 封面圖 | 建議 **1800×1200px**（3:2）｜JPG／WebP｜≤400KB |
| `news` | 內文插圖（編輯器內） | 建議寬度 **1800px**（3:2 最佳）｜JPG／WebP｜≤400KB |
| `certification` | 認證 Logo | 建議 **600×600px 去背 PNG 或 SVG**｜主體置中滿版、四周留白請先裁掉｜≤100KB |
| `client` | 客戶 Logo | 建議 **短邊 ≥300px 去背 PNG 或 SVG**｜≤100KB |
| `facility` | 設備照 | 建議 **1200×1200px**（1:1）｜JPG／WebP｜≤300KB |
| `vlog` | 縮圖覆蓋（選填） | 建議 **1280×720px**（16:9）｜未上傳將自動取 YouTube 官方縮圖 |
| `page`／`news`／`solution` | OG 分享圖（選填） | 建議 **1200×630px**（1.91:1）｜JPG／PNG｜≤300KB｜未上傳則沿用封面圖｜**僅此三個單元有此欄位**（§5.6；對應 `Page`／`News`／`Solution` 的 `OgImagePath`） |
| `supplier-notice` | 公告附件（選填） | PDF｜≤20MB |
| `supplier-download` | 下載檔 | PDF／XLSX／DOCX／ZIP｜≤20MB｜檔案類型與大小由系統自動帶入前台 |
| （前台）`get-a-quote` | 設計稿上傳 | PDF／AI／PSD／JPG／PNG／ZIP｜單檔 ≤20MB｜最多 5 個檔案 |

**共通規則**
- 每個圖片上傳欄位**必附一個多語 `Alt` 文字欄位**（中／英各一，必填）— 05-seo 硬性要求。
- 上傳後由後端驗副檔名白名單 + 檔頭 magic number，另存 WebP 衍生檔供前台使用（原檔保留）。
- 超過建議尺寸不擋，但後台顯示黃色提醒「圖片 3200×1200，超過建議尺寸，將由系統壓縮」。
- 尺寸不足一半時擋下並提示重新上傳（避免前台糊圖）。
- ⚠️ 認證 logo 現有素材僅約 152×165px（見 [IA §6](../reference/官網資訊架構_IA.md)），**上線前需向客戶索取向量原檔**。

---

## 4. 單元規格

> 以下每個單元的欄位表中：**語** 欄位標 ✓ 表示中英各存一份；**必** 表示必填。共用操作與 UI 規則見 §5。

### 00 `dashboard` 待辦總覽
唯讀首頁。四張數字卡：待處理報價（`Status IN ('New','InProgress')`）、待處理聯絡訊息（`Status = 'New'`）、**中英未對齊的內容筆數**、7 天內即將下架的內容。下方為最近 20 筆操作紀錄。點卡片直接跳到對應單元的已篩選清單。

### 01 `home-banner` 首頁 Banner
**前台**：`index.html` `#hero` 輪播（目前 3 張）。
**操作**：新增／編輯／拖曳排序／上下架／刪除。**建議 3–5 張**，超過 6 張時提示。

| 欄位 | 型別 | 必 | 語 | 說明 |
|------|------|----|----|------|
| 桌機圖 | 圖片上傳 | ✓ | | 見 §3 |
| 手機圖 | 圖片上傳 | | | 見 §3 |
| 圖片替代文字 Alt | 單行 | ✓ | ✓ | |
| 連結網址 | 單行 | | | 站內相對路徑或完整外部 URL |
| 另開視窗 | 開關 | | | 預設關 |
| 排序 | 拖曳 | | | |
| 上架／上架時間／下架時間 | 狀態組 | | | 見 §5.4 |

### 02 `solution` 解決方案
**前台**：`solutions.html` 列表、`products-boxes/cardboard/uv/other.html` 四個方案頁、首頁 Printing Solutions 四張卡。
**操作**：**固定 4 筆，不可新增／刪除**（新增方案屬改版範圍）；可編輯、排序、上下架。品項卡為子清單，可自由增刪。

主檔欄位：

| 欄位 | 型別 | 必 | 語 | 說明 |
|------|------|----|----|------|
| 代號 Code | 唯讀 | | | `boxes`／`cardboard`／`uv`／`other` |
| 方案名稱 | 單行 | ✓ | ✓ | 選單與卡片標題 |
| 頁面標題 H1 | 單行 | ✓ | ✓ | 如 Custom Color Box Packaging |
| 短述 | 多行(300) | | ✓ | 首頁／列表卡片用 |
| 方案導言 | 富文本 | | ✓ | 方案頁開頭段落 |
| 方案封面 + Alt | 圖片上傳 | ✓ | Alt ✓ | 見 §3 |
| SEO 欄位組 | — | | ✓ | 見 §5.6 |

品項子清單（`SolutionItem`）：品項圖 + Alt、名稱、說明（400 字內）、排序、上架。

### 03 `project` 案例實績
**前台**：`projects.html` `#cases` 卡片（可依分類篩選）。

| 欄位 | 型別 | 必 | 語 | 說明 |
|------|------|----|----|------|
| 分類 | 下拉 | ✓ | | 來源 `category`（Project） |
| 案例圖 + Alt | 圖片上傳 | ✓ | Alt ✓ | 見 §3 |
| 標題 | 單行 | ✓ | ✓ | |
| 說明 | 多行(400) | | ✓ | |
| 影片連結 | 單行 | | | 有值才顯示播放圖示 |
| 數據值／數據說明 | 單行 | | 說明 ✓ | 如 `-32%` ／ `carbon / unit`；兩者需同時填或同時空 |
| 排序、上下架 | — | | | |

### 04 `news` 最新消息
**前台**：`news.html` 列表 + 12 支詳細頁；`insights.html` 精選；首頁（若啟用）。
**清單**：可依分類／狀態／關鍵字篩選，欄位含縮圖、標題、分類、發佈日、狀態、**中/英完成度 badge**。

| 欄位 | 型別 | 必 | 語 | 說明 |
|------|------|----|----|------|
| 分類 | 下拉 | ✓ | | 來源 `category`（News） |
| 發佈日期 | 日期 | ✓ | | 前台顯示用（2026.03.13） |
| 封面圖 + Alt | 圖片上傳 | ✓ | Alt ✓ | 見 §3 |
| 標題 | 單行(250) | ✓ | ✓ | 同時作為 H1 |
| 摘要 | 多行(500) | ✓ | ✓ | 列表卡片 + 詳細頁導言 |
| 內文 | 富文本 | ✓ | ✓ | 允許：H3 小標、段落、粗體、清單、連結、圖片、引言 |
| 上首頁／精選 | 開關 | | | |
| OG 圖 | 圖片上傳 | | | 見 §3 |
| SEO 欄位組 | — | | ✓ | 見 §5.6 |

### 05 `vlog` Green Vlog

| 欄位 | 型別 | 必 | 語 | 說明 |
|------|------|----|----|------|
| YouTube 影片 ID／網址 | 單行 | ✓ | | 貼完整網址系統自動抽出 ID，並即時顯示縮圖預覽 |
| 分類 | 下拉 | ✓ | | 來源 `category`（Vlog） |
| 標題／說明 | 單行／多行 | 標題 ✓ | ✓ | |
| 縮圖覆蓋 | 圖片上傳 | | | 見 §3 |
| 設為頁面主打影片 | 單選開關 | | | **全站僅一支**；開啟新的會自動關閉舊的 |
| 排序、上下架 | — | | | |

### 06 `faq` FAQ
分類（選填，來源 `category`）／問題（單行 300）／答案（富文本）／排序／上架。清單支援依分類分組拖曳。
> 前台需輸出 `FAQPage` JSON-LD（05-seo §2.3），故問答需為純文字可解析結構，答案富文本避免使用表格。

### 07 `trend` 產業趨勢
`industry-trends.html` 的段落區塊：標題（單行 200）／內文（富文本）／排序／上下架。目前 5 筆。

### 08 `certification` 認證・夥伴・獎項
**前台**：首頁 Proof 認證牆、`about-certifications.html`、`differences.html`、`green-esg.html`。

| 欄位 | 型別 | 必 | 語 | 說明 |
|------|------|----|----|------|
| 分組 | 下拉 | ✓ | | 認證／合作夥伴／獎項（來源 `category`） |
| Logo + Alt | 圖片上傳 | ✓ | Alt ✓ | 見 §3 |
| 名稱／說明 | 單行／多行 | 名稱 ✓ | ✓ | |
| 連結網址 | 單行 | | | |
| 顯示於首頁 Proof 牆 | 開關 | | | 預設開；首頁建議 12–16 枚，超過時提示 |
| 排序、上架 | — | | | |

### 09 `client` 客戶 Logo
名稱（同時作為 alt，品牌名不翻譯）／Logo 上傳／連結／排序／上架。前台為輪播，建議 ≥6 筆。

### 10 `facility` 設備與廠房
**前台**：`facility-pre-press` ／ `-eco-printing` ／ `-post-press` ／ `-quality` ／ `-tour` 五個子頁的設備卡。

| 欄位 | 型別 | 必 | 語 | 說明 |
|------|------|----|----|------|
| 所屬子頁 | 下拉 | ✓ | | 印前／環保印刷／印後／品質檢驗／工廠導覽（來源 `category`） |
| 設備照 + Alt | 圖片上傳 | ✓ | Alt ✓ | 見 §3 |
| 設備名稱／說明 | 單行／多行(600) | 名稱 ✓ | ✓ | |
| 排序、上架 | — | | | |

> 五個子頁的**導言與流程敘述文字為固定**（§7），後台只管設備卡。

### 11 `job` 職缺
職稱（單行 160）／地點（單行）／說明（富文本）／排序／上下架（含自動下架日）。
> `careers.html` 的「Why NTI」六條福利為固定文案（§7）。

### 12 `supplier-notice` 供應商公告
分類（Policy／ESG／Quality／Logistics）／公告日期／標題／內文（富文本）／附件（PDF）／上下架。清單依日期倒序。

### 13 `supplier-spec` 規格與要求
標題（單行 160）／說明（多行 600）／排序／上架。目前 4 筆。

### 14 `supplier-download` 下載專區

| 欄位 | 型別 | 必 | 語 | 說明 |
|------|------|----|----|------|
| 檔案 | 檔案上傳 | ✓ | | 見 §3 |
| 顯示名稱 | 單行 | ✓ | ✓ | 如「Supplier Handbook 2026 (EN)」 |
| 檔案類型／大小 | 唯讀 | | | 上傳時自動帶入，前台顯示 `PDF｜2.4 MB` |
| 需登入下載 | 開關 | | | 會員系統上線（P6）後才生效，之前顯示「即將啟用」 |
| 下載次數 | 唯讀 | | | |
| 排序、上架 | — | | | |

### 15 `page` 頁面設定與 SEO
**29 筆固定頁**（28 筆既有 + 預留的 `green-csr`；清單見 [08-database.md §6.4](08-database.md)），**不可新增／刪除**，只可編輯。

| 欄位 | 型別 | 必 | 語 | 說明 |
|------|------|----|----|------|
| 頁面／路徑 | 唯讀 | | | |
| Slug | 單行 | ✓ | ✓ | 小寫、連字號；中英可不同 |
| SEO Title | 單行(70) | ✓ | ✓ | 即時字數計 + 超長警示 |
| Meta Description | 多行(180) | ✓ | ✓ | 同上 |
| Canonical | 單行 | | ✓ | 留空則自動 |
| OG 標題／描述／圖片 | — | | 文字 ✓ | 留空則沿用 SEO 欄位 |
| 允許索引 | 開關 | | | 關閉 → `noindex` |
| 頁面內容 | 富文本 | | ✓ | **僅 `HasRichBody = 1` 者顯示此欄位**：`privacy-legal`，以及預留的 `green-csr` |

### 16 `redirect` 301 轉址
來源路徑（唯一、小寫）／目標路徑／狀態碼（301／302／308）／啟用／命中次數（唯讀）。支援 **CSV 匯入匯出**（舊站 46 頁 + 80 篇文章的對照表）。儲存時檢查轉址鏈與迴圈並擋下。

### 17 `quote` 報價需求
**前台來源**：`get-a-quote.html`。**唯讀資料 + 可改狀態**，不可編輯客戶填寫內容。

- **清單**：報價單號、公司、聯絡人、產品類型、狀態、送出時間；可依狀態／日期區間／關鍵字篩選。
- **詳細**：完整表單內容（公司／聯絡人／Email／電話／產品類型／產業／數量／尺寸／材質偏好／期望日期／永續建議勾選／需求描述）＋ 設計稿附件下載（附件掃毒未通過者不提供下載）。
- **可改**：狀態（待處理／處理中／已報價／已結案／垃圾訊息）、承辦人、內部備註、標記已回覆。
- **匯出**：依篩選結果匯出 CSV（權限 `quote.export`，匯出行為寫入 `audit`）。
- **通知**：送出時寄業務通知信 + 客戶確認信，寄送結果記於 `EmailLog`，後台可查看失敗原因並重寄。

### 18 `contact` 聯絡訊息
同 17 的模式，欄位為姓名／Email／公司／電話／訊息；狀態為 待處理／已回覆／已結案／垃圾訊息。

### 19 `member` 會員管理（P6）
清單（Email／名稱／公司／狀態／註冊日／最後登入）、檢視、**啟用／停用**、重寄驗證信、觸發密碼重設信。**後台不可查看或設定會員密碼**。可從會員檢視頁跳至其報價與訂單。

### 20 `order` 訂單與生產進度（P6）
訂單：訂單編號／會員／關聯報價單／品名／狀態／預計出貨日。
進度：階段（設計／印前／印刷／印後／品檢／出貨）× 狀態（未開始／進行中／完成）＋ 時間＋備註，逐筆新增，會員中心以時間軸呈現。

### 21 `setting` 網站設定
單頁表單，依 `SiteSetting` 的固定 key 清單渲染（[08-database.md §6.3](08-database.md)），分四組：

- **公司資訊**：公司名稱、地址、營業時間（多語）；電話、傳真、Email、Google Map 嵌入碼。
- **社群**：Facebook／LinkedIn／YouTube 網址（留空則前台不顯示該圖示）。
- **首頁**：形象圖帶圖片 + Alt。
- **信件**：報價通知收件者、聯絡通知收件者、密件副本（支援多組，逗號分隔，儲存時驗格式）。

> 需求待補：`contact.html` 目前為台中暫代地址，上線前須換成台南實際廠址（見 [IA §7](../reference/官網資訊架構_IA.md)）。

### 22 `category` 分類管理
以 `CategoryType` 分頁籤管理九類分類（News／Project／Vlog／FAQ／認證／設備／供應商公告／產業別／報價材質）。欄位：代號（建立後唯讀）、名稱（多語）、排序、啟用。
**已被引用的分類不可刪除**，只能停用；刪除前顯示引用筆數。
> 產業別同時供 `projects.html` 的 Industries 清單與報價表單下拉使用；報價材質供報價表單下拉使用。

### 23 `admin` 管理員與角色
管理員：Email／顯示名稱／角色／啟用／最後登入；新增時寄啟用信，強制首次登入改密碼；連續 5 次失敗鎖定 15 分鐘。
角色：三個系統角色（超級管理員／內容編輯／檢視者，不可刪）＋ 權限矩陣勾選（§6）。
**不可停用或降級自己**；系統至少保留一名啟用中的超級管理員。

### 24 `audit` 操作紀錄
唯讀。兩個分頁：**操作紀錄**（時間／管理員／動作／對象／IP／變更明細 diff）與 **信件紀錄**（`EmailLog`，含失敗原因與重寄）。可依管理員／對象／日期區間篩選。保留 12 個月，逾期由排程清除。

---

## 5. 共用 UI 與行為規則

### 5.1 清單頁
分頁（預設 20 筆）、關鍵字搜尋、狀態篩選、**中/英完成度 badge**、批次上／下架、批次刪除（軟刪）。有 `SortOrder` 的單元提供拖曳排序，排序結果即時儲存。

### 5.2 編輯頁
左側為語系中性欄位（圖片、日期、分類、狀態），右側為 **中文／English 分頁**的文字欄位。切換語系不離開頁面、不遺失未存內容。離開前有未儲存變更時攔截。

### 5.3 多語
- 儲存時允許只填一種語系（存草稿），但**上架時必須兩語系皆完整**，否則擋下並指出缺漏欄位。
- 「複製中文到英文」按鈕，供先上架後翻譯的情境。

### 5.4 上下架
`上架` 開關 + `上架時間` + `下架時間`。三者組合的顯示狀態：草稿／已排程／上架中／已下架。時間以台北時間輸入、存 UTC。內容變更後由 webhook 觸發前台 ISR revalidate（05-seo §2.4）。

### 5.5 富文本編輯器
白名單標籤：`p h3 h4 strong em ul ol li a blockquote img figure figcaption br`。
禁止：`script iframe style` 與行內 `style`（YouTube 影片一律走 `vlog` 單元的欄位，不從編輯器貼 iframe）。
貼上 Word／網頁內容時自動清除樣式。伺服器端二次 sanitize，不信任前端。

### 5.6 SEO 欄位組（`page`／`news`／`solution`）
Slug、SEO Title（70 字提示）、Meta Description（180 字提示）、Canonical、OG 標題／描述／圖片。
Slug 由標題自動產生、可手改；重複時擋下。已上架內容改 slug 時，**自動在 `redirect` 建立一筆 301**。

### 5.7 刪除
一律**軟刪**（`IsDeleted = 1`），列表隱藏；系統設定類（分類、管理員）以停用取代刪除。刪除前確認對話框需顯示前台影響（例如「此分類有 8 篇新聞引用」）。

### 5.8 驗證與安全
必填／長度／格式（Email、URL、YouTube ID）皆前後端雙重驗證；後端為準。後台 SPA 以 `X-Robots-Tag: noindex` 排除索引，所有 `/api/v1/admin/*` 需 JWT + RBAC 檢查，寫入操作記 `AuditLog`。

---

## 6. 權限矩陣

| 動作 | 超級管理員 | 內容編輯 | 檢視者 |
|------|:---:|:---:|:---:|
| 內容單元 01–14 檢視 | ✓ | ✓ | ✓ |
| 內容單元 01–14 新增／編輯／排序 | ✓ | ✓ | — |
| 內容單元 01–14 上下架 | ✓ | ✓ | — |
| 內容單元 01–14 刪除 | ✓ | ✓ | — |
| 15 頁面 SEO ／ 16 轉址 | ✓ | ✓ | 檢視 |
| 17 報價 ／ 18 聯絡：檢視・改狀態 | ✓ | ✓ | 檢視 |
| 17 報價：附件下載・匯出 CSV | ✓ | — | — |
| 19 會員 ／ 20 訂單 | ✓ | — | — |
| 21 網站設定 ／ 22 分類 | ✓ | — | 檢視 |
| 23 管理員與角色 | ✓ | — | — |
| 24 操作紀錄 | ✓ | — | — |

權限碼 `{單元代號}.{view|edit|publish|delete|export}`，存於 `RolePermission`。上表逐格展開為種子列共 **171 列**（SuperAdmin 83／Editor 67／Viewer 21），見 [`db/seed/110_role_permission.sql`](../db/seed/110_role_permission.sql)；**本表為權限的權威來源**，[08-database.md §6.1](08-database.md) 僅為摘要。

矩陣描述到、但原本未定代號的三項（2026-09-02 補）：

| 權限碼 | 對應矩陣列 | 角色 |
|---|---|---|
| `quote.download` | 17 報價：附件下載 | 僅 SuperAdmin |
| `redirect.export` | 16 轉址：CSV 匯入匯出 | SuperAdmin、Editor |
| `audit.resend` | 24 操作紀錄：`EmailLog` 重寄 | 僅 SuperAdmin |

`SuperAdmin` 亦逐列展開、不使用 `system.*` 之類的萬用碼——RBAC 檢查邏輯保持單一（一律查 `RolePermission`）且可稽核。

---

## 7. 不做後台的固定文字區（決議 3 的明細）

下列內容**寫死在前端程式碼**，修改需走改版流程（開發者調整 + 重新部署）。這些頁在後台仍有 `page` 單元可改 SEO。

| 頁面／區塊 | 內容 | 不做後台的理由 |
|---|---|---|
| 首頁 What We Do（4 格） | Structural Design／Pre-Press (CTP)／Printing／Finishing | 品牌簡報定案文案，屬品牌定位不隨檔期變動 |
| 首頁 Why global brands choose NTI?（6 格） | Direct Delivery … One Trusted Partner | 同上 |
| `about-difference` / `about-benefits` / `about-certifications` 敘述段落 | 品牌長文 | 品牌敘事，改動頻率以年計 |
| `green-our-advantage` / `green-carbon` / `green-materials` / `green-esg` 敘述段落 | 綠色永續長文（含水處理數值等） | 同上；數據更動屬改版等級 |
| `facility*` 五個子頁的導言與流程說明 | 製程敘述 | 同上（設備卡本身有後台，見單元 10） |
| `solutions` / `projects` / `insights` hub 頁的導言與導覽卡 | 區塊標題與引導文 | 結構性文案，與版型綁定 |
| `contact` / `get-a-quote` 表單標籤與說明文字 | 欄位標籤、隱私同意文字 | 與表單驗證邏輯綁定，改文案需同步改程式 |
| 各頁 banner 裝飾大圖 | 版型背景圖 | 屬視覺設計資產，非內容 |
| 頁首選單、頁尾連結、浮動按鈕 | 站台結構 | 由 IA 決定，改動＝改架構 |

> **擴充路徑**：若客戶日後要求自行維護上述任一段落，最小改動是在 `Page` 表把該頁 `HasRichBody` 設為 1，並將該區塊改讀 `PageI18n.BodyHtml` — 不需要改動 schema。

---

## 8. DoD

> 僅列本文件獨有項；SEO／安全／效能的通則見 [`03-backend.md` §5](03-backend.md)。

- [ ] §2 的 24 個單元皆可獨立操作，無跨單元耦合。
- [ ] **每個上傳欄位旁都顯示 §3 的建議尺寸提示文字**，且每個圖片欄位都有中英 Alt。
- [ ] 無「媒體管理」選單；刪除資料列即解除檔案引用。
- [ ] §7 清單中的區塊確認為前端寫死，未誤做成後台欄位。
- [ ] 上架時強制中英兩語系完整，缺漏欄位明確指出。
- [ ] §2.1 的三個缺口已取得客戶書面確認（納入或不納入）。

---

## 變更紀錄

| 日期 | 修改者 | 摘要 |
|------|--------|------|
| 2026-09-01 | Tim（Claude Code） | 初版：依 mockup 44 頁與三條決議（單元式後台／無 Media Library／固定文字不進後台）定義 24 個後台單元、上傳尺寸總表、權限矩陣與固定文字清單 |
| 2026-09-02 | Tim（Claude Code） | §3 上傳總表的 OG 分享圖列由「各單元」更正為 `page`／`news`／`solution`（與 §5.6 及 `OgImagePath` 實際欄位一致）｜配合 [`db/`](../db/) 建置腳本落地：§2.1 三個待確認缺口補記「schema 已預留」對照表（範圍與估算不變）；§6 權限矩陣確立為權威來源並補 `quote.download`／`redirect.export`／`audit.resend` 三個權限碼、註明展開後共 171 列；§15 固定頁 28 → 29 筆（含預留的 `green-csr`），富文本欄位不再僅限 `privacy-legal` |

*最後更新：2026-09-02*
